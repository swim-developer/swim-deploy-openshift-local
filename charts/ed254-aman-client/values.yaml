namespace: swim-demo
appName: ed254-aman-client

swimNamespace: swim-demo

backend:
  enabled: true
  image:
    repository: quay.io/masales/example-apps-ed254-aman
    tag: backend
    pullPolicy: Always
  replicas: 1
  port: 8093
  resources:
    requests:
      memory: "256Mi"
      cpu: "100m"
    limits:
      memory: "512Mi"
      cpu: "500m"
  env:
    javaOpts: "-Xmx768m -XX:+UseParallelGC"
    logLevel: "INFO"
  livenessProbe:
    path: /q/health/live
    initialDelaySeconds: 30
    periodSeconds: 30
    timeoutSeconds: 5
    failureThreshold: 3
  readinessProbe:
    path: /q/health/ready
    initialDelaySeconds: 10
    periodSeconds: 10
    timeoutSeconds: 5
    failureThreshold: 3

frontend:
  enabled: true
  image:
    repository: quay.io/masales/example-apps-ed254-aman
    tag: frontend
    pullPolicy: Always
  replicas: 1
  port: 8080
  resources:
    requests:
      memory: "32Mi"
      cpu: "50m"
    limits:
      memory: "64Mi"
      cpu: "100m"
  env:
    backendUrl: http://ed254-aman-client-backend:8093
  route:
    enabled: true
    host: ed254-aman-client.apps-crc.testing
    tls:
      enabled: true
      termination: edge
      insecureEdgeTerminationPolicy: Redirect

ed254Provider:
  serviceName: swim-ed254-provider
  port: 8080
  internalPort: 9080

kafka:
  serviceName: kafka-kafka-bootstrap
  port: 9092
  topics:
    arrivalSequence: ed254-arrival-sequence-topic
    providerException: ed254-provider-exception-topic

labels:
  app.kubernetes.io/name: ed254-aman-client
  app.kubernetes.io/instance: ed254-aman-client
  app.kubernetes.io/part-of: swim-framework
  app.kubernetes.io/component: example-app
  app.kubernetes.io/managed-by: helm
